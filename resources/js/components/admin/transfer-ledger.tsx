import { Form, router } from '@inertiajs/react';
import { FormEvent, useState } from 'react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { formatCurrency, formatDateTime } from '@/lib/money';

type Transfer = { id: number; type: string; status: string; amount: string; fee: string; currency: string; customer: string; customer_email: string; source_account: string; recipient: string | null; bank_name: string | null; description: string | null; created_at: string | null };
type Props = { transfers: { data: Transfer[]; links: Array<{ url: string | null; label: string; active: boolean }>; total: number }; filters: { status: string; type: string } };

export default function TransferLedger({ transfers, filters }: Props) {
    const [status, setStatus] = useState(filters.status);
    const [type, setType] = useState(filters.type);
    function filter(event: FormEvent) { event.preventDefault(); router.get('/admin/transfers', { status: status || undefined, type: type || undefined }, { preserveState: true, replace: true }); }
    return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Transfer ledger</h1><p className="mt-1 text-sm text-muted-foreground">Review and correct completed customer transfers while keeping linked balances and fees synchronized.</p></div><form onSubmit={filter} className="flex flex-wrap gap-2"><select value={status} onChange={(event) => setStatus(event.target.value)} className="h-9 rounded-md border border-input bg-background px-3 text-sm"><option value="">All statuses</option><option value="completed">Completed</option><option value="pending">Pending</option><option value="failed">Failed</option></select><select value={type} onChange={(event) => setType(event.target.value)} className="h-9 rounded-md border border-input bg-background px-3 text-sm"><option value="">All types</option><option value="internal">Internal</option><option value="domestic">Domestic</option><option value="wire">Wire</option></select><Button type="submit">Filter</Button></form><Card><CardHeader><CardTitle>Transfers</CardTitle><CardDescription>{transfers.total} records</CardDescription></CardHeader><CardContent className="grid gap-4">{transfers.data.map((transfer) => <div key={transfer.id} className="rounded-xl border p-4"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold capitalize">{transfer.type} transfer</p><Badge variant={transfer.status === 'failed' ? 'destructive' : transfer.status === 'completed' ? 'default' : 'secondary'} className="capitalize">{transfer.status}</Badge></div><p className="mt-1 text-sm text-muted-foreground">{transfer.customer} · {transfer.source_account} → {transfer.recipient ?? 'Recipient'}</p><p className="text-xs text-muted-foreground">{formatDateTime(transfer.created_at)}{transfer.bank_name ? ` · ${transfer.bank_name}` : ''}</p></div><div className="text-right"><p className="font-semibold">{formatCurrency(transfer.amount, transfer.currency)}</p><p className="text-xs text-muted-foreground">Fee {formatCurrency(transfer.fee, transfer.currency)}</p></div></div><Form action={`/admin/transfers/${transfer.id}`} method="patch" onSubmit={(event) => { if (!window.confirm('Save this transfer correction? Linked balances and the mandatory fee may change.')) event.preventDefault(); }} className="mt-4 grid gap-3 border-t pt-4 sm:grid-cols-2">{({ processing, errors }) => <><div className="grid gap-2"><Label htmlFor={`description-${transfer.id}`}>Transfer description</Label><Input id={`description-${transfer.id}`} name="description" defaultValue={transfer.description ?? ''} placeholder="Reference or description" /><p className="text-xs text-destructive">{errors.description}</p></div><div className="grid gap-2"><Label htmlFor={`amount-${transfer.id}`}>Amount ({transfer.currency})</Label><Input id={`amount-${transfer.id}`} name="amount" type="number" min="0.01" step="0.01" defaultValue={transfer.amount.replace(/[$,]/g, '')} disabled={transfer.status !== 'completed'} required /><p className="text-xs text-destructive">{errors.amount}</p></div><div className="flex items-end justify-end sm:col-span-2"><Button disabled={processing || transfer.status !== 'completed'}>{processing ? 'Saving…' : 'Save transfer changes'}</Button></div>{transfer.status !== 'completed' && <p className="text-xs text-muted-foreground sm:col-span-2">Only completed transfers can have their amount corrected.</p>}</>}</Form></div>)}{transfers.data.length === 0 && <p className="py-10 text-center text-sm text-muted-foreground">No transfers match these filters.</p>}<div className="flex flex-wrap gap-2">{transfers.links.map((link, index) => <Button key={index} size="sm" variant={link.active ? 'default' : 'outline'} disabled={!link.url} onClick={() => link.url && router.get(link.url)} dangerouslySetInnerHTML={{ __html: link.label }} />)}</div></CardContent></Card></div>;
}
